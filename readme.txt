14-Oct-2020
Shannon Spires

This is ping for Clozure Common Lisp.

It creates an ICMP packet, sends it, and waits for a response.

I wrote this so I could use CCL to write a program to monitor my
Internet connectivity since my ISP is a bit flaky.

Only works in CCL for now but it could be ported to other Common Lisps.